<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/oc_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentLeft="true"
    android:layout_alignParentStart="true"
    android:clickable="true"
    android:elevation="2dp"
    android:foreground="?attr/selectableItemBackground"
    android:longClickable="true"
    android:stateListAnimator="@animator/touch_rise_item"
    android:transitionName="card"
    app:contentPadding="8dp">

    <RelativeLayout
        android:id="@+id/oc_rl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginBottom="8dp"
        android:layout_marginTop="8dp">

        <ImageView
            android:id="@+id/oc_folder_icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            app:srcCompat="@drawable/ic_folder_red"/>

        <TextView
            android:id="@+id/oc_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toEndOf="@+id/oc_folder_icon"
            android:layout_toRightOf="@+id/oc_folder_icon"
            android:paddingLeft="16dp"
            android:textAppearance="@style/TextAppearance.AppCompat.Large"
            tools:text="Dossier"/>
    </RelativeLayout>

</android.support.v7.widget.CardView>

